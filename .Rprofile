# Raise the timeout the language server passes to callr (default ~120s → 300s)
options(languageserver.server_timeout = 300)
# Suppress renv startup messages to reduce initialization overhead
options(renv.config.startup.quiet = TRUE)

source("renv/activate.R")
